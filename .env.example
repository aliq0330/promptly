# Supabase — not yet wired up (see CLAUDE.md section 8, module 18+).
# Copy this file to .env.local once a Supabase project exists and fill in
# the values from your project's API settings. Never commit .env.local.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Never expose this to the client or commit it.
SUPABASE_SERVICE_ROLE_KEY=
